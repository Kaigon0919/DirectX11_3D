
#pragma once

#include "Macro.h"

ENGINE_BEGIN

enum SCENE_CHANGE
{
	SC_NONE,
	SC_NEXT
};

enum SHADER_TYPE
{
	ST_VERTEX,
	ST_GEOMETRY,
	ST_PIXEL,
	ST_END
};

enum COMPONENT_TYPE
{
	CT_TRANSFORM,
	CT_RENDERER,
	CT_USERCOMPONENT,
	CT_CAMERA,
	CT_MATERIAL,
	CT_COLLIDER,
	CT_LIGHT,
	CT_UI,
	CT_PARTICLE,
	CT_LANDSCAPE,
	CT_ANIMATION,
	CT_ANIMATIONFRAME,
	CT_NAVIGATION,
	CT_ARM,
	CT_SOUND,
	CT_TEXT
};

enum AXIS
{
	AXIS_X,
	AXIS_Y,
	AXIS_Z,
	AXIS_END
};

enum CONSTANT_SAHDER
{
	CS_VERTEX = 0x1,
	CS_PIXEL = 0x2,
	CS_GEOMETRY = 0x4
};

enum KEY_STATE
{
	KS_PRESS,
	KS_PUSH,
	KS_RELEASE,
	KS_END
};
enum KEY_STATE_FLAG
{
	KSF_PRESS = 0x1,
	KSF_PUSH = 0x2,
	KSF_RELEASE = 0x4
};
enum SKEY
{
	SKEY_CTRL,
	SKEY_ALT,
	SKEY_SHIFT,
	SKEY_END
};
enum SKEY_ENABLE
{
	SE_CTRL = 0x1,
	SE_ALT = 0x2,
	SE_SHIFT = 0x4
};


enum LOOKAT_AXIS
{
	LOOK_X,
	LOOK_Y,
	LOOK_Z,
	LOOK_ALL,
	LOOK_END
};

enum CAMERA_TYPE
{
	CT_PERSPECTIVE,
	CT_ORTHO,
	CT_END
};

enum RENDER_STATE
{
	RS_RASTERIZER,
	RS_DEPTH,
	RS_BLEND,
	RS_END
};

enum LIGHT_TYPE
{
	LT_DIR,
	LT_POINT,
	LT_SPOT,
	LT_END
};

enum RENDER_GROUP
{
	RG_LANDSCAPE,
	RG_DEFAULT,
	RG_ALPHA,
	RG_HUD,
	RG_UI,
	RG_END
};

enum RENDER_MODE
{
	RM_FORWARD,
	RM_DEFERRED
};

enum ANIMATION_OPTION
{
	AO_LOOP,
	AO_ONCE_DESTROY
};

enum COLLIDER_TYPE
{
	CT_SPHERE,
	CT_CAPSULE,
	CT_OBB,
	CT_POINT,
	CT_RAY,
	CT_AABB
};

enum COLLISION_CHANNEL_STATE
{
	CCS_IGNORE,
	CCS_OVERLAP,
	CCS_BLOCK
};

enum COLLISION_CALLBACK_STATE
{
	CCS_BEGIN,
	CCS_STAY,
	CCS_LEAVE,
	CCS_END
};

enum ANIMATION_FRAME_TYPE
{
	AFT_ATLAS,
	AFT_FRAME,
	AFT_END
};
enum MOUSE_BUTTON
{
	MB_NONE = -1,
	MB_LBUTTON,
	MB_RBUTTON,
	MB_MBUTTON,
	MB_END
};

ENGINE_END

